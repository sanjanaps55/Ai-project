FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY scripts/livekit-agent-worker.mjs scripts/

ENV NODE_ENV=production

ENTRYPOINT ["node", "scripts/livekit-agent-worker.mjs"]
CMD ["--room", "nova-lobby"]
